[package]
name = "canopy"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
gix = { version = "0.81", features = ["merge"] }

[dev-dependencies]
tempfile = "3.13"
